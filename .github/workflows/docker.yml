# Disabled in this fork — upstream Docker images are published by dundee/gdu.
# This fork only provides arm64 binary packages via arm64-release.yml.
name: Docker

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "yes" to run (disabled by default in this fork)'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docker workflow is disabled in this fork."
