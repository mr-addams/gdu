# Disabled in this fork — upstream releases are managed by dundee/gdu.
# This fork publishes arm64-only packages via arm64-release.yml.
name: Release

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "yes" to run (disabled by default in this fork)'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is disabled in this fork."
