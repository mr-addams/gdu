# Disabled in this fork — no code changes, Codecov token not available.
# Tests run in upstream dundee/gdu.
name: run tests

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "yes" to run (disabled by default in this fork)'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test workflow is disabled in this fork."
