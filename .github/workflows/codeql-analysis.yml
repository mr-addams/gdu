# Disabled in this fork — no code changes, analysis runs in upstream dundee/gdu.
name: "CodeQL"

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "yes" to run (disabled by default in this fork)'
        required: true

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL workflow is disabled in this fork."
